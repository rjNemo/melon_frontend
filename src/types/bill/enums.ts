export enum Room {
  t2,
  t3
}

export enum Platform {
  web,
  booking,
  airbnb,
  tripadvisor,
  perso
}

export enum PaymentMethod {
  card,
  cash,
  cheque,
  transfer
}

export enum PaymentStatus {
  pending,
  canceled,
  completed,
  refund
}
